name: forgd_core
channels:
  - defaults
dependencies:
  - pip=24.0
  - python=3.10.14
  - pyyaml=6.0
  - setuptools=69.5.1
  - wheel=0.43.0
  - pip:
      - pytest
      - pytest-asyncio
      - pytest-mock
      - flask
      - flask-openapi3
      - flask-openapi3[swagger]

prefix: /opt/anaconda3/envs/condor
